{
  "name": "password-manager",
  "version": "1.0.0",
  "description": "manage your password",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
    "start":"node apps.js"
  },
  "author": "pandu",
  "license": "ISC",
  "dependencies": {
    "node-persist": "0.0.6"
  }
}
